import units from './units';
// margins & paddings
const sizes = {
  colorField: {
    height: 1.75 + units.rem,
  },
  inputField: {
    height: 1.75 + units.rem,
  },
  radioButton: {
    height: 1.75 + units.rem,
  },
  borderInput: {
    borderRadius: 0.25 + units.rem,
  },
  borderButton: {
    borderRadius: 1 + units.rem,
  },
  borderReduxButton: {
    borderRadius: 0 + units.rem,
  }
};

export default sizes;
