import units from './units';
// margins & paddings
const margins = {
  fieldMargin: {
    paddingLeft: 0.3 + units.rem,
    paddingRight: 0.3 + units.rem,
  },
  radioMargin: {
    paddingLeft: 0.3 + units.rem,
    paddingRight: 0.3 + units.rem,
  },
  buttonMargin: {
    paddingLeft: 0.3 + units.rem,
    paddingRight: 0.3 + units.rem,
    border: units.none
  }
};

export default margins;
